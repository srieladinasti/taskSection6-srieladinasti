fun main() {
    println("Short Circuiting in kotlin is only evaluate the first argument, if the first argument of logical AND is false then the entire expression will always be false")
    println("Also, if the first argument of logical OR is true then the entire expression will always be true")
    // example:
    // var umur = 19
    // var status = single

    // if (umur < 19 && status == "Single"){
    // println("Lanjutkan Pendaftaran")}
    // else { println ("Pekerjaan yang anda lamar tidak sesuai dengan kualifikasi yang telah ditentukan")}

    // if (umur >= 19 || status = "Menikah"){
    // println("Lanjutkan Pendaftaran")}
    // else { println("Pekerjaan yang anda lamar tidak sesuai dengan kualifikasi yang telah ditentukan")}

    println()
    println("Scope is starts with an opening curly brace followed by closing curly brace, then inside the scope we create a variable or a constant to evaluates")
}